# dependencies (frontend needs none; server/ uses @anthropic-ai/sdk)
node_modules/

# secrets — server-side API key lives here and must NEVER be committed
.env
server/.env
*.env.local

# logs & os cruft
*.log
.DS_Store
Thumbs.db
desktop.ini

# editor
.vscode/
.idea/
*.swp

# local scratch
/tmp/
*.tmp
